apiVersion: v1
kind: Service
metadata:
  name: book-service
  namespace: bookstore-ns
spec:
  type: ClusterIP
  selector:
    app: book-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
