apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: bookstore-ns
spec:
  type: ClusterIP
  selector:
    app: crm-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
